# deps / build
node_modules/
.next/
dist/
.turbo/
pnpm-debug.log*

# python
.venv/
.cache/
__pycache__/
*.pyc
*.egg-info/
.poetry/

# env / secrets
.env
.env.*
!.env.example

# recordings / runtime
recordings/
*.mp4
*.webm
*.wav

# editor / os
.DS_Store
*.log
.idea/
.vscode/

# test
coverage/
*.tsbuildinfo

# secrets manual (contains live API keys — never commit)
docs/API使用手册.md

# third-party clones in research (borrowed locally, NOT our code — don't commit)
research/ai-mock-interview/
research/interview-video-tools/
research/reference/

# harness self-play 产出（回归语料输出，不入库）
apps/agent/harness/run_out/

# researcher runtime cache (live API search results; not source)
apps/agent/src/agent/researcher/researcher_cache.json

# runtime data lives on the data disk (settings.data_dir = /data/probedesk),
# never on / — nothing to ignore here for that path; keep old source-tree
# store ignored so a stray legacy file is never committed.
apps/agent/src/agent/data/store.json

# deployment private key (never commit; only the .pub is committed)
deploy/*_ed25519
