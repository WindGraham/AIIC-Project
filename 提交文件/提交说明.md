# 提交说明 · AI 模拟面试官（2026-08-30 挑战）

> 提交方式：8/30（周日）**24:00 前**汇总为**一封邮件**发送至 **mlic@pku.edu.cn**。
> 以邮件服务器时间戳为准；截止后不接收任何提交；**截止后服务器上的任何构建/部署都判超时**。

---

## 一、必交清单核对表

| # | 必交项 | 状态 | 说明 |
|---|--------|------|------|
| 1 | **3 分钟 Demo 视频** | ⚠️ **待录制**（最重要缺口） | 放 `demo视频/demo_video.mp4`；一镜到底即可；脚本见 `demo视频/demo视频脚本.md` |
| 2 | **可访问的产品链接** | ✅ 已上线 | URL：<https://mock.windgraham.art>（Nginx 反代 :3101 + HTTPS；agent:8000） |
| 3 | **Product Memo（1–2 页）** | ✅ 已完成 | `Product-Memo.md` |
| 4 | **GitHub 代码仓库**（public + 清晰 commit） | ✅ 已 public | <https://github.com/WindGraham/AIIC-Project>；40 个干净 commit |

## 二、产品链接 & 访问说明（提交邮件里需写明）

- **公网 URL**：<https://mock.windgraham.art>（在线至 8/30 24:00）
- **测试账号**：⚠️ 需填写 —— 项目为登录制。请**注册一个 demo 账号**后把账号/密码填到下方（用脱敏信息，不要用真实个人账号）。
  - 账号：________　密码：________
- **API / 部署限制说明**：产品使用 DeepSeek / Gemini / Volcengine STT / MiniMax TTS / LiveKit；无 key 时前端仍可渲染（mock-first 降级），语音/看屏/判码需对应 key（已配置）。如需说明可在邮件里补充。

## 三、SSH 公钥（主办方要求加 2 把，否则视为产品不可访问）

- 需把 PDF 脚注里的 **2 把 SSH 公钥**加到云服务器 `~/.ssh/authorized_keys`（登录后查看运行环境与最后部署时间）。
- 服务器已生成一把本机公钥：`deploy/aiic_ed25519.pub`（可直接提交；**私钥已 gitignore，绝不上传**）。
- ⚠️ **请确认**：PDF 里的那 2 把主办方公钥**是否已加入到服务器**。这是硬性要求，缺了会判"产品无法访问"。

## 四、Demo 视频录制指引（最重要缺口）

- 放入：`demo视频/demo_video.mp4`（≤ 3:00）
- 脚本：`demo视频/demo视频脚本.md`（已给出 0:00–3:00 分镜，**前 30 秒 wow = "我们做的是 AI 面试官，不是 AI 答题" + AI 语音开场**）
- 录制前：用脱敏测试账号登录 → `/resumes` 存一份干净脱敏简历 → 预约"某互联网大厂 · AI 应用开发（财经方向）"（用 `docs/JD样例-脱敏.md` / `docs/简历样例-脱敏.md`）→ 进房间 → 报告。

## 五、可选加分材料

- `调研记录.md`（10 位访谈）：已放入本文件夹。
- 建议补一张**架构图**（质量 > 数量；当前缺）。
- 仓库内 `docs/` 已有：开发流程、API 清单、产品方案、部署与演示、剩余任务等。

## 六、截止前待办（按优先级）

1. **录 demo 视频**（必交，最要紧，需真机录屏+配音）。
2. **确认 2 把主办方 SSH 公钥已加服务器**（必交，硬性）。
3. **写测试账号**到本说明第 2 节（必交，登录制产品）。
4. **push 本地领先 origin 的 2 个提交** + 确认 GitHub 默认分支为最新代码（GitHub 默认分支当前为 master，本地为 main，注意对齐）。
5. 把本文件夹里的材料 + 公网 URL/账号/SSH 说明，汇总成**一封邮件**发至 mlic@pku.edu.cn。
